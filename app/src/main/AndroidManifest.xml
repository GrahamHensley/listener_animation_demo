<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools" package="com.listener.animations">

    <application android:allowBackup="false"
                 android:label="@string/app_name"
                 android:icon="@mipmap/ic_launcher"
                 android:roundIcon="@mipmap/ic_launcher_round"
                 android:supportsRtl="true"
                 android:theme="@style/AppTheme"
                 android:name=".AnimationsApp" tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".activity.ValueAnimatorActivity"/>


        <activity android:name=".activity.SwipeActivity" />
        <activity android:name=".activity.BoringActivity" />

        <activity android:name=".activity.IntoActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>
